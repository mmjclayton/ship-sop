# Security Review — Add Uninstall section to README

Date: 2026-04-25
Diff range: e82ccd5..HEAD (27 added lines)
Files changed: README.md
Reviewer: security-reviewer

## Summary

Documentation-only change. Adds an Uninstall section with shell command examples for removing ship-sop install artifacts. No code path, no auth surface, no input handling, no third-party integrations introduced.

## Critical Findings

None.

## High Findings

None.

## Medium / Low Findings

None.

The `rm ~/.claude/agents/{...}.md` brace expansion is safe — no user input enters the shell. The `jq 'del(...)' settings.json > settings.json.tmp && mv ...` pattern is the standard safe-edit-via-tempfile idiom; no risk of clobbering on partial write.

## Recommendations

None. The example commands document destructive operations (`rm`, `rm -rf .ship/`) — readers should be aware that these are irreversible. The `.ship/` deletion is benign (it's a runtime artifact directory and the README states this) but a one-line caution above the project-scope block would be polite.

This is a Low-severity hygiene note, not a blocking finding.

Verdict: APPROVE
