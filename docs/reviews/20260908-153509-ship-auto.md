Covers: 66d77828284264f2382d1544703dc34e1492624f

# Codex publication gate — ship-sop

## Summary

PASS with non-blocking follow-ups. No unresolved HIGH or CRITICAL findings.
Severity: MEDIUM

P28 and P30: native Codex installation, skills, isolated reviewers, shared hook policy,
explicit enforcement port and a simpler public README. Full review range:
`d364711e963a9470152fb06dbdce07d3d272dc41..66d77828284264f2382d1544703dc34e1492624f`.

Coverage is cumulative: all configured reviewers inspected the full port through
`ce6a43f`, then reviewed fixes in `ce6a43f..5cc4879` and `5cc4879..66d77828284264f2382d1544703dc34e1492624f`.
Historical blocking findings below were corrected in those independently reviewed
deltas. This report does not relabel an earlier review as covering a later commit.

| Reviewer | Blocking threshold | Final fix review | Outstanding blocking findings |
|---|---|---|---|
| code-reviewer | HIGH | PASS | 0 |
| security-reviewer | CRITICAL | PASS | 0 |
| silent-failure-hunter | HIGH | PASS | 0 |

## Findings and dispositions

- Fixed replica validation bypasses in `scripts/validate-state-transitions.sh`:
  structural JSON parsing handles inline empty exclusions; Codex uses user-owned
  baselines; project exclusions cannot suppress native asset checks; missing user
  configuration fails when project replication configuration exists. Stale-mirror,
  project-shadowing and missing-configuration fixtures verify these paths.
- Fixed `setup.sh` and `scripts/setup-codex.sh` registration checks: stale hook
  paths cannot be accepted as working auto-mode or retire the legacy handler.
- Fixed earlier review findings: trusted user-installed reviewer executable and
  policy; disabled external reviewer integrations; source-link and customization
  preservation; safe priority marker validation; checked asset discovery; native
  instruction failures remain visible and conservatively retain code enforcement.
- Enforcement changes are explicitly declared under agent-sop P109 / ship-sop P30.
  Independent source reviews and the established pre-port validator provide
  evidence beyond results from the modified enforcement layer itself.
- MEDIUM follow-ups remain in agent-sop P108: sync/install ownership bookkeeping,
  shared instruction symlinks and the documented legacy migration limitation.
  ship-sop P29 tracks the symlink replica fix; P31 tracks empty ownership records
  preserving artifacts after Codex-then-Claude uninstall. None reaches its
  configured blocking threshold. No claim is made that these are fixed.

## Validation

- All eight agent-sop fixture suites pass, including 30 state-transition cases.
- ship-sop installation/isolation fixtures pass, including hostile project runner,
  conflicting verdicts, forced dual-runtime config preservation and stale hooks.
- Shell syntax, relevant ShellCheck checks, configuration/frontmatter checks and
  README local links pass. Existing customized global reviewer profiles remain.
- Pre-port validator passed the final Backlog transitions; its original 29 state
  fixtures passed. It incorrectly skipped the new inline-exclusion stale-mirror
  case; the fixed validator rejects that case as expected.
- Real native replication checks pass after synchronization (20 changed tracked
  files upstream). The parent ran suites; read-only reviewers performed source
  analysis in independent clones, using the trusted installed runner.

## Live runtime feedback

User-provided live evidence confirmed project-root hook payloads, exec_command
normalization to Bash/command, and production Stop continuation with the expected
tracker-drift notice. SessionStart, UserPromptSubmit and Stop were also observed
in a disposable runtime. A raw exec_command fixture was not evidence of a
translation bug. A complete automatic review-to-push cycle is not claimed by
those lifecycle tests. No merge or release is authorized by this gate report.

## Independent review evidence

The following are verbatim source-review results. Earlier BLOCK verdicts are
historical and their findings are resolved by the subsequent reviewed fixes above.

### Full port: code-reviewer

## Review Summary

Source analysis completed for `d364711..ce6a43f`. Test execution remains with the parent.

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 2 | info |
| LOW | 0 | pass |

## Findings

[MEDIUM] Priority refresh breaks shared instruction symlinks  
File: `scripts/refresh-priorities.sh:186`  
Issue: `mv "$OUTPUT" "$CLAUDE_MD"` replaces a symlink with a regular file. When `AGENTS.md` links to shared instructions, the target stays stale and subsequent updates diverge between runtimes. Already tracked in P29.  
Fix: Resolve the destination symlink before creating and replacing the temporary file. Preserve the link and the target’s permissions.

[MEDIUM] Removed Codex installation permanently changes Claude uninstall behaviour  
File: `setup.sh:400`  
Issue: The preservation check tests only whether `ship-sop.install.json` exists. `scripts/install-codex.sh:71` retains this file even after uninstall removes every asset, writing `{}`. Consequently, uninstalling Codex and then Claude still forces `KEEP_CONFIG` and `KEEP_ARTIFACTS`, leaving configuration and runtime artifacts despite requesting complete removal.  
Fix: Remove an empty installation manifest after uninstall, or check for remaining installed assets before forcing preservation. Add coverage for uninstalling Codex before Claude.

Verdict: PASS

### Full port: security-reviewer

Source analysis completed for `d364711..ce6a43f`. No actionable security findings identified.

Reviewed command construction, installer filesystem operations, reviewer isolation, trusted policy loading, verdict validation and secret patterns. No hardcoded secrets or dependency manifests requiring an npm audit were found.

Tests and live sandbox enforcement were not executed; those remain with the parent as requested. No files were changed or artifacts written.

Verdict: PASS

### Full port: silent-failure-hunter

- **HIGH - `setup.sh:520`**: The new unified-hook check accepts any command containing `sop-stop-drift.sh` without checking that the referenced script exists. A stale registration passes, removes the legacy project handler and prints that auto-mode uses agent-sop hooks, although the replacement cannot execute. **Fix:** validate the registered command and its script before removing the existing handler; fail with an actionable diagnostic if validation fails.

- **MEDIUM - `scripts/refresh-priorities.sh:186`**: `mv "$OUTPUT" "$CLAUDE_MD"` replaces an instruction-file symlink with a regular file, then reports successful refresh. For shared `AGENTS.md` or `CLAUDE.md` symlinks, the actual shared target remains stale and future updates diverge. This is already recorded as P29 but remains present. **Fix:** resolve the symlink target and atomically replace that target while preserving the link.

Source analysis completed. No files written or test suites run; test execution remains with the parent.

Verdict: BLOCK

### First fix delta: code-reviewer

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

## Findings

No issues - source analysis completed for the specified range.

- `setup.sh:521` checks the unified hook file and exact registration before removing the legacy handler.
- `scripts/validate-state-transitions.sh:478` prevents project exclusions from suppressing user-scope mirror checks.
- `tests/codex-install.sh:108` verifies stale registration rejection preserves project settings.
- `Backlog.md:567` explicitly declares the validator changes under P30, satisfying the gate-integrity scope exception.

Tests were not run, as instructed; execution belongs to the parent.

Verdict: PASS

### First fix delta: security-reviewer

Security review completed for `ce6a43f..5cc4879`.

Severity: NONE. No security findings in the scoped diff.

- `setup.sh:522` checks the expected hook file and exact registered command before retiring the legacy handler.
- `scripts/validate-state-transitions.sh:378` uses user-scope Codex configuration; line 479 prevents project exclusions from suppressing runtime mirror checks.
- Secret scans found no credentials. No dependency manifests or dependency changes required auditing.

Source analysis only. Test execution remains with the parent; no artifacts were written.

Verdict: PASS

### First fix delta: silent-failure-hunter

- **HIGH | scripts/validate-state-transitions.sh:378**
  - **Issue:** Missing Codex user configuration becomes a successful skipped check.
  - **Evidence:** The new assignment unconditionally selects the user configuration and prevents project discovery at line 380. If that file is missing, lines 384-386 exit successfully, even when `.codex/agent-sop.config.json` contains tracked baselines.
  - **Impact:** An incomplete installation or incorrect `CODEX_HOME` can bypass replication validation while reporting that the project does not track replicas.
  - **Suggested fix:** Keep user configuration authoritative, but fail with a clear diagnostic when it is missing and project replication configuration exists. Only skip successfully when replication is genuinely unconfigured.

Source analysis completed for all three changed files. No test suites run; test execution remains with the parent.

Verdict: BLOCK

### Final configuration fix: code-reviewer

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

## Findings

Severity: NONE

No issues - `scripts/validate-state-transitions.sh:385` correctly blocks when project Codex configuration exists but the selected user configuration is missing. The four-line change preserves existing behaviour for Claude and unconfigured projects.

The range contains only the declared validator fix, so the gate-integrity exception applies. Source analysis completed; test execution remains with the parent.

Verdict: PASS

### Final configuration fix: security-reviewer

Severity: NONE

No security findings in the requested diff. At `scripts/validate-state-transitions.sh:385`, the added guard fails closed when Codex project configuration exists but user configuration is missing. Variables remain quoted; the change introduces no command execution, secret exposure or dependency changes.

Source analysis completed. Test execution remains with the parent.

Verdict: PASS

### Final configuration fix: silent-failure-hunter

No findings. The new guard at `scripts/validate-state-transitions.sh:385-388` reports missing user configuration to stderr and exits with status 1, preventing the previous successful skip when project Codex configuration exists.

Source analysis completed for the requested diff and surrounding configuration handling. Tests remain with the parent.

Verdict: PASS
